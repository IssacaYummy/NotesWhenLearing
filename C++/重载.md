# 函数的重载

实质：用同样的名字在定义一个有着不同参数但有着同样用途的函数，可以是参数个数上的不同，也可以是参数数据类型上的不同

对函数进行重载，事实上可以简化编程工作和提高代码的可读性

注意：

1. 对函数（方法）进行重载一定要谨慎，不要无的放矢。因为重载函数越多，该程序就越不容易看懂

2. 注意区分重载和覆盖

3. 我们只能通过不同参数进行重载，但不能通过不同的返回值进行重载

4. 对函数的重载的目的是为了方便对不同数据类型进行同样的处理
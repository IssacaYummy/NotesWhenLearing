容器：能容纳两个或更多个值的数据结构我们称为容器（conatiner）

  

迭代器

迭代器是一种功能非常有限却非常实用的函数，提供一些基本操作符：*、++、==、！=、=

迭代器是个所谓的智能指针，具有遍历复杂数据结构的能力

因为迭代器的功能是如此基本，所以标准库里的每一种容器都支持，当在程序里改用另一种容器的时候就用不着修改那么多代码了

每一种容器都必须提供自己的迭代器，事实上每种容器都将其迭代器以嵌套的方式定义于内部

因此各种迭代器的接口相同，实现不同